### Visibility

参数说明

| 类型   | 默认值 | 示例                |
| ------ | ------ | ------------------- |
| `bool` | `true` | `visibility(false)` |

#### Native

```dart
 Visibility(
  visible: false,
  child: Text("some text"),
);
```

#### Kit

```dart
Text("some text").visibility(false);
```
